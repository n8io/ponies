.wager-wrapper(data-ng-class='{won: wager.status == "PAID" && wager.payoutAmount > 0,lost: wager.status == "PAID" && wager.payoutAmount == 0,pending: wager.status == "PLACED",canceled: wager.status == "CANCELED"}')
  gravatar(email='wager.user.email', size='20', round='', title='{{wager.user.firstName + " " + wager.user.lastName}}', default='identicon', letter='wager.user.firstName')
  .wager-type.hide-xs(data-ng-bind='wager.poolTypeName')
  .wager-type.hide-sm.hide-md.hide-lg.hide-xl(data-ng-bind='wager.poolType')
  .wager-selections(data-ng-bind='wager.selections')
  .wager-amount(data-ng-if='wager.status != "PAID" && wager.status != "CANCELED"', data-ng-bind='wager.betAmount | currency:"$":2')
  .wager-payout.loser(data-ng-show='wager.payoutAmount === 0', data-ng-if='wager.status == "PAID"', data-ng-bind='wager.betAmount | currency:"$":2')
  .wager-payout.winner(data-ng-show='wager.payoutAmount > 0', data-ng-bind='(wager.betAmount | currency:"$":2) + "/" + (wager.payoutAmount | currency:"$":2)')
  .wager-refund(data-ng-if='wager.status == "CANCELED"', data-ng-bind='wager.betAmount | currency:"$":2')
  .float-right
    wager-timestamp.hide-xs(wager='wager')
  .clearfix
